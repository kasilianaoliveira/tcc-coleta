import React from 'react'

export const index = () => {
  return (
    <div>
      {/* <div>
        <label>Bairro(s)</label>

        <ul>
          {pointInfo.neighborhoods && pointInfo.neighborhoods.map((neighborhood, index) => (

            <li key={index}>
              <div>
                <div className="neighborhood-list">
                  <strong className='text-neighborhood'>
                    {neighborhood.name} -
                  </strong>

                  <p> Dias da semana: {neighborhood.daysOfWeek.join(', ')}</p>
                  <BiSolidTrashAlt size={25} className="remove-task-button"
                    onClick={() => handleRemoveNeighborhood(neighborhood.name)} />

                </div>
              </div>
            </li>
          ))}
        </ul>
      </div>
      <div>
        <ul className="items-grid-edit">
          {pointList.pointItems && pointList.pointItems.map((pointItem) => (
            <li key={pointItem.item.id}>
              <img src={pointItem.item.image_url} alt={pointItem.item.title} className='items-image-edit' />
              <span>{pointItem.item.title}</span>
            </li>
          ))}
        </ul>
      </div> */}
    </div>
  )
}
